// 配置项

// const host = "http://localhost:5757";
const host = "https://yseacbku.qcloud.la";

const config = {
    host,
    loginUrl:`${host}/weapp/login`,
    usernUrl:`${host}/weapp/user`
}

export default config